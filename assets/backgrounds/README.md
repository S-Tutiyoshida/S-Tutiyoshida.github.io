# Background images

Put images for the random page background in this folder.

Supported formats: `.jpg`, `.jpeg`, `.png`, `.webp`, `.avif`, `.gif`.

Every time a page loads, one image from this folder is selected at random.

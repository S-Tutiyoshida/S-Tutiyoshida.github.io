# Background images

Put random background images in this folder.

Supported formats: `.jpg`, `.jpeg`, `.png`, `.webp`, `.avif`, `.gif`.

Each page load picks one image at random from this folder first.
